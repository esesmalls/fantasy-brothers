# GitHub 同步记录

## 2026-09-20 · U47静态半身比例修订

从既有 `codex/h-motion-templates` 的 `8d2046b` 接续，推送前核对远端同分支仍为该基线；不覆盖其他电脑历史。同步新侧身图集、冻结旧比例的对照配置、实际导出帧、提示/哈希与标准。根目录入口仍为 `Review-Static-Bust.cmd`；构建EXE不进入Git，已有旧包的电脑拉取后运行 `tools/build_static_bust_review.ps1` 重建。正常战役与个人存档不变，本轮不合main、不发布Release。最终提交以Git历史为准。

更新：2026-09-19。

## 第三台电脑 0.1.2 接续

本轮基线 `edb6370`，开始交付时 fetch 核对远端 main 未变化。使用 `codex/battlefield-hud` 完成全屏战场 HUD、悬浮信息、图示、回归和大地图设计，验证后提交并快进 main，同步至原仓库。最终哈希以 Git 历史为准；不发布 Release。

本地试玩包 `builds/FantasyBrothers-0.1.2-windows.zip` 不入 Git；其他电脑可复制压缩包或拉取后按 README 重建。个人存档仍不经 Git 自动同步，0.1.2 不改变 0.1.1 规则和存档结构。

## 第三台电脑 0.1.1 接续

本轮基线为 `88c55c8`，实施前与收尾时均只读核对远端 main 相同，未覆盖其他电脑的提交。本轮包含战斗可读性、友军通行、两路线与旧档兼容；本地使用 `codex/battle-readability-routes` 隔离交付提交后快进 main，同步至原仓库。最终哈希以 Git 历史为准，不在同一提交中自引用哈希。未发布 Release。

试玩包为 `builds/FantasyBrothers-0.1.1-windows.zip`，不会随源码推送；其他电脑可复制该压缩包或拉取后重建。个人存档也不经 Git 同步，转移规则见 `docs/10-framework-roadmap.md`。

## 目标

将本项目同步到 GitHub 用户 `esesmalls` 名下的仓库 `fantasy-brothers`。当前远端已由用户创建为公开仓库；后续如需隐藏源码，再单独调整可见性。

## 本地准备

- 当前工作区已初始化本地 `.git`，分支为 `main`，已完成首个实现提交与同步记录提交。继续工作时先核对本地更改和远端历史，不能直接以远端覆盖本地文件。
- `.gitignore` 排除了 Godot 编辑器下载包、导出模板、构建输出、Godot 生成状态、个人存档与常见凭据文件。
- `tools/fetch_windows_templates.py`、`tools/godot/SHA512-SUMS.txt` 和导出模板的 `source.json` 保留，便于追溯工具来源与重新下载。
- `.gitattributes` 统一文本文件换行，并将媒体和二进制标为二进制文件。

## 认证与远端状态

截至本记录更新时，GitHub 连接器已确认当前授权用户为 `esesmalls`，并确认 `esesmalls/fantasy-brothers` 已创建、可写，默认分支为 `main`。仓库当前为公开可见性，保留用户创建时的设置；本机已完成首次提交并推送，CLI 凭据不写入项目。

首次同步完成后，按以下顺序复核：

1. 查询 `esesmalls/fantasy-brothers` 是否存在及其可见性。
2. 确认本地 remote 指向 `https://github.com/esesmalls/fantasy-brothers.git`。
3. 提交前确认待提交清单不含被排除的下载包、模板、构建物、存档或凭据。
4. 推送 `main` 后核对远端提交哈希与本地一致。

已记录提交哈希、远端 URL 和因忽略规则未同步的本地文件；不得把认证信息写入本文件或仓库。

## 首次同步结果

- 提交：`0f02560`（Build playable prototype vertical slice）
- 远端：`https://github.com/esesmalls/fantasy-brothers.git`
- 分支：`main`
- 已排除：`builds/`、Godot 可执行文件与导出模板、`.godot/`、运行日志和本地存档。

2026-09-18 主任务接续复核：本地 HEAD、origin/main 与实时查询远端 main 均为 `025e8af891c933c0b2699264b4b0bb2575486b5e`，工作树当时干净。接受已提交的存档规范化和 AI 绕行修复，以该提交为基线完成交付检查。后续维护提交以 Git 历史为准，避免在同一提交的文件中记录自身哈希。

2026-09-19 / 0.1.3：以10161e4为基线，codex/world-travel-loop实现旅行闭环；源码、规则测试、验证记录与三机说明一起交接。构建物和QA截图保留本机builds目录，个人存档不上传。提交后按快进方式同步main并核对远端，最终哈希查Git历史。

2026-09-19 / 0.1.4：基线63ed359，开发分支codex/equipment-inventory。接入库存/买卖/自由武器组合与共享分层人物、伤势；最终实际导出283/0、32截图。源码、规则/UI测试与交付记录入库，builds/与QA/个人存档保留本机；提交后核对远端无新冲突再快进同步main，最终提交号查Git历史。

2026-09-19 / 人物成长与传承研究：基线0708516，分支codex/character-growth-design。新增官方日志研究、人物属性/培养/伤残/性格提案，以及用户最新约60–80游戏日对应15年的长期培养参照。只同步设计、决策与导航文档；游戏仍为0.1.4，没有新构建或存档修改。文档链接与差异检查通过后，按原流程快进同步main；最终提交号以Git历史为准。

本轮实际状态：用户随后明确允许同步，设计提交580071f及状态提交6d9507e已推送main，实时远端核对为6d9507e6f45f42176fd183d0cb7831417ec14f7e。此前自动审批拦截已由本次明确授权解决，没有改用其他渠道。用户同时授权启动人物属性增量，并确认高出身高雇佣/维护费用的后续经济方向。

2026-09-19 / 0.1.5：开发基线02a9ef2、分支codex/character-progression。按本次明确授权同步人物属性/升级/训练、跨出身能力、人物帐、存档迁移及验证资料；新增历史测试夹具来自项目历史测试生成器，不含用户个人存档。最终检查后快进main并推送，实时核对远端哈希；最终提交号查Git历史。构建包、引擎、模板、QA截图与个人进度不上传，其他电脑使用0.1.5代码重建或复制本地试玩ZIP。

2026-09-19 / 0.1.5.1：基线5f80529，开发分支codex/compact-battle-inspection。交付默认紧凑悬浮、Alt详情、图标/色条、点击防穿透和四步批次计划；真实导出340/0。沿用用户GitHub同步授权，检查后快进main、推送并核对远端；构建物、截图、引擎与个人存档不上传，最终提交以Git历史为准。

2026-09-19 / 美术选型研究：基线1d10856，仅新增17美术方式比较并更新导航、状态与决策。沿用文档同步授权，差异与链接检查后同步main并核对远端，最终提交查Git历史。推荐方法待用户同意，没有新资产、游戏改动、工具安装或新试玩包；四步实现仍按最新授权边界等待方法确认。

2026-09-19 / 美术详细计划与战后幸存：基线aeae553。用户已确认制作方式，交付18详细计划、设定集分析、现有内容盘点/验收/三机资产交接方案，并将“倒地后战胜可能重伤幸存”并入16第四步。只同步本项目文字计划与导航/状态；用户PDF、截图及渲染预览不入库，游戏/存档/正式资产不变，仍无新构建。沿用文档同步授权，检查后推送main并核对远端，最终提交以Git历史为准。

2026-09-20 / 四套美术候选：基线185f871。最新U33明确要求设定集原件上传，此要求覆盖上一条“PDF不入库”的历史约定。同步references/battle-brothers中的原始60页PDF及来源/哈希，art/validation/2026-09-19-four-directions中的48张候选PNG、提示词/清单/不足、离线评审页与独立复核，以及状态/导航/决策更新。A/C/D07及D12使用标明的替代稿，候选未通过正式像素装配，游戏/存档/导出包不变。图像共91,262,326字节，PDF11,295,974字节；未纳入个人存档、工具二进制、浏览器测试脚本或QA截图。首次OpenSSL连接失败，单次命令采用Windows schannel后fetch恢复，未修改全局Git配置。提交前核对origin/main仍为185f871，按已有授权推送main，最终哈希由Git历史与远端核验。

2026-09-20 / 第二轮四套美术候选：基线6085c81。按U34–U35同步art/validation/2026-09-20-refined-directions的47张PNG（78,359,898字节）、原提示/来源/哈希、反馈与独立复核、13槽位离线评审页，以及导航/状态/计划/决策。E07恢复用户认可的原始输出；四套12和H07共5项被工具拦截，明确未生成。顺带纠正上一轮D套覆盖统计，不覆盖旧图片。设定集已在上一提交同步，本次不重复复制；游戏、存档和导出包均未变。47图加载、缺项跳过、灯箱键盘、评分保存、复制文本生成、桌面/窄屏及离线入口验证通过；浏览器脚本/截图留在忽略的builds目录，不纳入个人数据或工具缓存。按已有同步授权检查远端、提交推送main并核对实时远端哈希；最终提交查Git历史。Git命令单次采用Windows schannel，不改变全局配置。
2026-09-20 / H主导风格定案：基线be95ba9，按U36记录E/F/G/H评分4/1/4/4与H主导、少量E/G质感的正式方向。交接19标准、18计划入口、反馈/状态/导航、H三状态修订页及3张最终PNG（3,474,105字节），保留提示、来源、哈希及独立复核。内置image_gen共6次；07依据QA从H06重新派生，修同脸同装和去底座。三张均有透明alpha，但像素装配、边缘、动作和游戏导出未验收。沿用既有项目同步授权，提交推送main并核对远端，最终提交以Git历史为准；个人存档、本机备份、构建物、未采用图与工具缓存不入库。内嵌浏览器通过本地HTTP验证页面；file协议被浏览器策略阻止，未绕过，也不把离线运行记为实测通过。

2026-09-20 / U45主AI独立复审：基线8a508e4，实时远端codex/h-motion-templates一致。仅同步23审阅报告、最新验收退回状态及22/A批次说明更正；原图、运行代码、main与Release均不变。握点诊断脚本和JSON留本机builds/a-standard-audit，核心方法和数值记录于23；未重跑全套历史测试或重新导出。沿用云端接力授权，审阅文档提交到现有动作分支并核对远端，最终提交号见Git历史。
2026-09-20 / U46静态半身：以44a25d1为基线，主AI独立完成24标准、新图集原件与过程稿、生成提示／哈希、Godot独立评审器和110张实际导出截图、123项专项与301项UI验证。静态网页24项离线检查通过。本机Windows包可由tools/build_static_bust_review.ps1重建，根目录Review-Static-Bust.cmd启动；构建物不入库。同步codex/h-motion-templates，main及Release不变，最终哈希查Git历史。当前样板待用户审美评审，不视为全库放行。
