# Fantasy Brothers 项目协作约定

## 用户目标

- 原创 2D 战棋与佣兵团经营游戏，长期目标为 Steam。
- 真人只有用户一人，每周投入 10–20 小时；使用 AI 小团队分工。
- 战斗与剧情兼顾，魔幻中世纪画风，重视视觉与动画质量。
- 长期扩展技能、人物成长、佣兵团类型和发展特性，包含驯兽方向。
- 事件与角色成长融入肉鸽变化，强化 PTR 式技能联动、元素反应与场景互动。
- 用户已确认佣兵团在战役内持续成长，远征随机事件与奖励，失败承担伤亡和损失。
- 先把少量机制做深，再增加数量。用户最新指令优先于设计草案。

## 开始工作

先读 `docs/STATUS.md`、`docs/03-decisions-and-sources.md`，再读取与任务相关的设计和实际工程。`README.md` 是导航，`docs/00-product-and-roadmap.md` 是总体方案，`docs/04-systems-expansion.md` 是肉鸽、联动与环境规则提案；研究报告备选规则不自动覆盖采用方案。

区分用户已确认要求、可验证提案与已实现行为。规划中的内容不等于已批准的具体数值、预算或发售日期。只推进当前用户请求范围，不因存在任务清单就自动进入下一阶段。

## AI 分工

主 AI 负责拆任务、接口、集成、项目状态与统一汇报。专项职责定义在 `.codex/agents/`：`combat_engineer`、`content_designer`、`visual_designer`、`qa_reviewer`。

当用户要求按 AI 团队执行工作时，对能独立推进的明确子任务使用相应子代理；最多同时 3 个子代理，简单任务直接处理。依赖同一接口或修改同一文件的工作串行。角色按任务运行，不宣称后台常驻或跨会话自动继续。

委派时给出目标、必读资料、允许修改范围、依赖和验收标准。每个文件同一时间只有一位写入负责人。子代理返回改动、验证证据、未解决问题；主 AI 负责最终集成。

## 工程与内容原则

- 规则状态与表现分离；预览和结算共用规则，动画不决定伤害或资源扣除。
- 固定版本与随机状态，故障能在同版本复现；保存稳定 ID 和存档版本。
- 技能、装备、单位、事件采用明确的数据结构；先满足已验证需求，再抽象扩展。
- 新机制必须有玩家价值、成本、反制、表现与验证场景。
- 技能、反应与场景共用状态和触发约束；保存根行动、次数及期限，预防循环触发。
- 随机事件与成长先筛选合法候选、提供回退，生成后保存；不得重复领奖或因菜单重开重抽。
- 可死亡人物的事件要有死亡／缺席路径；主线不得无意绑定永久死亡的单一队员。
- 资产记录来源、授权与生成工具；按统一样板检查动作、方向、装备叠层和可读性。
- 新增规则做有意义的验证；实际试玩使用导出包。未运行的验证要明确标出。
- 技术栈目前是提案，实施时先查现有工程，不擅自维护多个引擎版本。

## 交付与接力

小步交付，每两周目标是一个可玩的增量。交付说明写清给玩家带来的变化、如何验证与已知问题；AI 生成量不作为进度指标。

任务结束更新 `docs/STATUS.md`。关键决定写入决策记录；大型设想进入候选池。长期记忆依赖文件、版本与测试记录。

对用户用中文和清晰的产品语言沟通。必要的技术细节写入工程文档，优先给用户可以试玩或直接评审的结果。
