# 第二轮评审页验证

2026-09-20。本机使用捆绑Playwright和Microsoft Edge无头浏览器，独立临时浏览器上下文，无需安装依赖，不访问用户浏览器资料。应用浏览器自动化入口初始化失败，采用本地浏览器测试；不是Godot导出试玩。

## 实际结果

- 13个状态逐一加载：每状态4图，07为3图，12为0图，共47张不同PNG成功读取；图片HTTP错误0、页面脚本错误0。
- 按套13张目标卡片，E/F/G各12张图片、H11张；5个缺项只有明确说明，不请求不存在的图片。
- 灯箱左右键、Escape关闭通过；H逐套循环访问11个有效图，跳过07和12，并回到首图。
- 128像素选项的4个整图盒均为128×128；深浅背景切换通过。该尺寸包含原图留白，不声称角色实际高度相同。
- 修正长画布预览被正方形卡片裁掉底托的问题；最终默认视图4张图片边界均完整位于卡片内，原图文件未改。
- 在隔离上下文设置评分和中文备注，重新加载仍保留；新批次使用独立v2存储键。
- 点击复制按钮后拦截网页Clipboard接口，核对第二轮标题、评分和备注文本正确；没有写入用户系统剪贴板，真实系统剪贴板权限未实测。
- 1440×1100桌面与390×844窄屏无横向溢出，完成截图目视复核；file://离线入口默认4图加载通过。

本机证据：`builds/art-v2-qa/results-final.json`、`desktop-final.png`、`mobile-final.png`；执行脚本`builds/art-v2-final-qa.cjs`。这些可重建QA产物不入Git。原始PNG和提示记录入库，文件元数据见[独立复核](metadata-review.json)。

图片边缘、绘制一致性、倒伏重量感及动作适用性见[视觉复核](REVIEW.md)和各套QA。网页交互通过不表示模块装配、动画或游戏内表现已经通过。
