# U51 · 装配台 v2 验证

2026-09-21，主AI独立执行。技术验证通过不代表美术小样已获用户定稿认可。

## 发现并修正

1. 初接旧裸身时宽63、肩线过高，双肩从衣服外露；用户同时指出身体明显偏大。改为宽52；用户随后要求侧向修订、反馈侧过并给出参考及短颈要求，最终局部重绘为轻侧短颈。保留用户衣物／武器校准，最终像素比较证明两侧不再露出身体。
2. 首次血迹位置被下巴遮住，调整为胸侧位置；现在裸身可见，衣料可真实遮挡。
3. 原 JSON schema 数字解析为浮点，数组成员类型检查误拒schema2；改用数值比较，并加保存／重载校验。
4. 新导出测试一次类型推断错误导致窗口无法执行；已修正并重建。以下结果全部来自最终修正版，未把失败中间版当作完成证据。

## 最终验证

| 检查 | 结果 |
| --- | --- |
| 装配文档与基本交互 | 68 / 0 |
| 旋转、父子变换、存取与弓箭连续性 | 83 / 0 |
| 静态人物／武器回归 | 147 / 0 |
| 游戏主入口 smoke | 301 / 0 |
| Windows 实际鼠标／键盘操作回归 | 29 / 0 |
| Windows 新拆件操作与渲染 | 31 / 0 |
| Windows 原嵌套穿戴回归 | 85 / 0 |
| 评审网页切换、资源链接、窄屏 | 26 / 0 |

新拆件导出检查包含：载入真实 `my_test.json`、旋转数值与撤销重做、Alt拖动、超出旧±20范围的实际拖动、单次操作一步撤销、找回移远部件、部件启用／位置保存重载、1440×960和1180×740窗口。像素比较确认血迹在皮肤上、衣物覆盖、伤痕／血迹移出所属部件后被遮挡，衣外双肩不再暴露。

`captures/` 中11张截图来自最终轻侧短颈版 Windows 导出（`builds/u51-visual-v4`）。本页包括主界面、裸身、净面、伤痕、发须绷带、穿衣／甲、旋转联动、小窗口与两张实际局部渲染。未使用图片修补这些截图。

引擎：Godot4.7.2 stable ed1daf0bf，Windows兼容渲染，RTX4080 Laptop。导出摘要及原画来源见[manifest](manifest.json)。网页通过本机8791访问，用Edge/Playwright核查。

## 保存与原画保护

- 原 `my_test.json` SHA256仍为 `9f4c1a16a80204e424171d621ec18acffc217bc78a896485d979a56aea4382d4`。
- 原 U50 catalog SHA256仍为 `b411dfd57a4efb9bbf1c866f2d37f8dbfbc351d838431da5403719e1c678123e`；原PNG不覆盖。
- 新模块注册、生成图集、用户校准副本与拆件示例独立保存；CLI验证用户schema2副本通过，无警告。
- 附着图集一次内置imagegen成功，RGBA1536×1024；首次身体补图一次被审查拦截。用户随后追加朝向与短颈反馈，两次现有身体区域编辑成功，最终只采用轻侧短颈身体。详见[prompts](prompts.md)。

## 仍待美术工作

身体已经按最后参考回调为轻侧身与浅颈连接，方向／比例仍需用户目视复评。只有一个净面与一套发须、伤痕、绷带、血迹，未证明不同脸型、头盔、衣甲血迹及全部伤损兼容。身体、头部或衣物被用户任意大幅调整后仍可能穿插，工具的扩大范围不是美术自动合身保证。方向母版与第二身份通过之前不批量重制。

不改战役、装备规则、0.1.5.1存档、全库美术或既定无手静态路线；不运行子智能体。
